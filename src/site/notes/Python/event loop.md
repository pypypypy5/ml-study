---
{"dg-publish":true,"permalink":"/python/event-loop/"}
---

**이벤트 루프**

async 함수에서, await 붙은 실행은 미뤄두고 다음으로 넘어감.

**코루틴**: **`async def`**로 정의된, **`await`**로 중단/재개 가능한 함수

코루틴은 이벤트 루프에 쌓이고, 메인 스레드에서 작업 다 끝나면 이벤트 루프에 제어권을 준다.

간단히, 공통적으로 _**“`await` = 이 함수의 나머지 코드를 나중에 재개하도록 예약하는 키워드”라고 생각해도.**_