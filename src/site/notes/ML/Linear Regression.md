---
{"dg-publish":true,"permalink":"/ml/linear-regression/"}
---

$$w = (X^T X)^{-1} X^T y$$
- X (데이터): 매 행이 데이터, 열이 feature들에 해당
- **$y$ (정답 레이블)**: $(n \times 1)$ $\rightarrow$ 집값들
- **$w$ (우리가 구하려는 가중치)**: $(p \times 1)$ $\rightarrow$ (평수 가중치, 방개수 가중치)