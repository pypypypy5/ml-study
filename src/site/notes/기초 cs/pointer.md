---
{"dg-publish":true,"permalink":"/cs/pointer/"}
---

int x = 10

int *p = &x

하는 식으로 *붙여 포인터 만들고, &로 해당 변수 주소 할당. 이제 p는 0x001… 식으로 사용하면 주소가 가져와진다.

이후 *p = 20 하는 식으로 사용하면 역참조, 포인터가 할당받은 주소의 위치에 값을 assign해 “값” 변경.